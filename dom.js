//  DOM - The DOM (Document Object Model) is a representation of a web page that browsers create when a website loads. It turns the HTML into a tree-like structure where every element (like headings, buttons, and images) becomes a node that JavaScript can access and manipulate.

// to get elemetns from DOM and to add elements to the DOM(page)

// What is DOM?
// 4 Pillars of DOM
// Simple example of bulb or whatever clicking on the same button
// Multiple events
// selecting multiple elements at a same time
// set Timeout
// setlnterval
// future projects


//// 4 pillars of DOM
// 1. Selecting elements
// 2. changing HTML
// 3. changing CSS
// 4. listening to events